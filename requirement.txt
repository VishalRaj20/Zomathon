pandas>=1.5
numpy>=1.23
scikit-learn>=1.2
lightgbm>=3.3          # preferred — remove if not available
xgboost>=1.7           # fallback option — remove if not available
fastapi>=0.100
uvicorn[standard]>=0.22
pydantic>=2.0